{
  "name": "coral-compare",
  "version": "1.0.0",
  "private": true,
  "description": "Coral visual comparison tool with a stateless backend proxy (no database).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
